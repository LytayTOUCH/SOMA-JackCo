%table.table
  %thead
    %tr
      %th.col-md-1 Code
      %th.col-md-1 Type
      %th.col-md-3 Stage
      %th.col-md-2 Date
      %th.col-md-2 Active
      %th.col-md-1 Actions
  %tbody
    - @coconuts.each do |coconut|
      %tr
        %td= coconut.code
        %td= coconut.coco_type
        %td= coconut.stage.name
        %td= coconut.planting_date.strftime('%Y-%B-%d')
        %td
        - if coconut.active?
          = "active"
        - else  
          = "inactive"
        %td
          - if can? :update, coconut
            = link_to "Edit", edit_coconut_path(coconut)
          = '|'  
          = link_to "Show", coconut_path(coconut)
.pull-right
  = paginate @coconuts