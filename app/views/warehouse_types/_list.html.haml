%table.table
  %thead
    %tr
      %th.col-md-8= "Name"
      %th.col-md-3= "Active"
      %th.col-md-1= "Actions"
  - @warehouse_types.each do |warehouse_type|
    %tr
      %td= warehouse_type.name
      %td
        - if warehouse_type.active?
          = "Actived"
        - else
          = "Inactived"
      %td
        / - if can? :update, warehouse_type
        = link_to "Edit", edit_warehouse_type_path(warehouse_type)
.pull-right
  = paginate @warehouse_types
