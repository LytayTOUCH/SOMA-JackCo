%table.table
  %thead
    %tr
      %th.col-md-4= "Name"
      %th.col-md-8= "Description"
  - @warehouse_types.each do |warehouse_type|
    %tr
      %td= warehouse_type.name
      %td= warehouse_type.description
.pull-right
  = paginate @warehouse_types
