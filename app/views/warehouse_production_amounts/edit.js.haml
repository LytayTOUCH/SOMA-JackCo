:plain
  $(".form-modal").html("#{escape_javascript(render 'adjustment_modal', locals: @warehouse_production_amount)}");
  $('#myModalAdd').modal('show');
  $('#myModalAdd').on('shown.bs.modal', function () {
    $('#new_amount').focus();
  });
