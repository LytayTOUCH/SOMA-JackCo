.row
  %h3
    = "Resource"
    .pull-right
      - if can? :create, Resource
        = link_to "New Resource", new_resource_path, class: "btn btn-primary"
  %hr
  / = render partial: 'list_resource', locals: { resources: @resources }

