:plain
  $(".form-modal").html("#{escape_javascript(render 'warehouse_item_transaction_edit_modal', locals: @warehouse_item_transaction)}");
  $('#myModalAdd').modal('show');
  $('#myModalAdd').on('shown.bs.modal', function () {
    $('#reason_text_area').focus();
    
    initialize();
  });
